---
phase: 06-secrets-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - auth/google_sheets.py
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "config.py exposes get_google_credentials() that returns a dict from st.secrets or None"
    - "config.py exposes has_google_credentials() that returns bool"
    - "CREDENTIALS_FILE constant does not exist anywhere in config.py"
    - "auth/google_sheets.py get_client() uses gspread.service_account_from_dict() with credentials from st.secrets"
    - "oauth2client import does not exist anywhere in auth/google_sheets.py"
    - "oauth2client does not appear in requirements.txt or any source file"
  artifacts:
    - path: "config.py"
      provides: "Credential helpers replacing CREDENTIALS_FILE constant"
      contains: "get_google_credentials"
    - path: "auth/google_sheets.py"
      provides: "gspread client using service_account_from_dict"
      contains: "service_account_from_dict"
    - path: "requirements.txt"
      provides: "Dependency list without oauth2client"
  key_links:
    - from: "auth/google_sheets.py"
      to: "config.py"
      via: "import get_google_credentials"
      pattern: "from config import.*get_google_credentials"
    - from: "auth/google_sheets.py"
      to: "gspread"
      via: "service_account_from_dict"
      pattern: "service_account_from_dict"
---

<objective>
Replace oauth2client-based Google Sheets authentication with Streamlit's native st.secrets system and gspread.service_account_from_dict().

Purpose: This is the core credential migration — removes the deprecated oauth2client dependency entirely and establishes st.secrets as the single source of truth for Google API credentials.
Output: config.py with credential helpers, google_sheets.py using service_account_from_dict, requirements.txt without oauth2client.
</objective>

<execution_context>
@C:/Users/omarh/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/omarh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-secrets-management/06-RESEARCH.md
@config.py
@auth/google_sheets.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite config.py credential helpers</name>
  <files>config.py</files>
  <action>
Remove the `CREDENTIALS_FILE = "credentials.json"` constant and the `# ─── Credentials ───` section entirely.

Add the following imports at the top of config.py (after existing imports):
```python
from typing import Optional, Dict
import streamlit as st
```

Add a new section `# ─── Credentials ──────────────────────────────────────────` (replacing the old one) with two functions:

1. `get_google_credentials() -> Optional[Dict]`:
   - Wrap `st.secrets["google_credentials"]` access in `try/except` catching `StreamlitSecretNotFoundError` (from `streamlit.errors`) and `KeyError` and `TypeError`.
   - On success, convert to plain dict via `dict(st.secrets["google_credentials"])`.
   - Validate the dict has `type == "service_account"` and a non-empty `private_key`.
   - Return the dict if valid, `None` otherwise.
   - On any exception, return `None`.

2. `has_google_credentials() -> bool`:
   - Simply returns `get_google_credentials() is not None`.

IMPORTANT: `st.secrets.get()` and `'key' in st.secrets` raise `StreamlitSecretNotFoundError` when no secrets file exists — always use try/except.
Do NOT import `StreamlitSecretNotFoundError` at module level inside config.py (it may fail on older Streamlit); instead catch the generic exception or import it inside the function.
  </action>
  <verify>
    1. `grep -c "CREDENTIALS_FILE" config.py` returns 0
    2. `grep -c "get_google_credentials" config.py` returns at least 2 (definition + potential internal use)
    3. `grep -c "has_google_credentials" config.py` returns at least 1
    4. `python -c "from config import get_google_credentials, has_google_credentials; print('OK')"` succeeds
  </verify>
  <done>config.py exports get_google_credentials() and has_google_credentials(). CREDENTIALS_FILE constant is gone. No oauth2client references anywhere in config.py.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite google_sheets.py to use service_account_from_dict + update requirements.txt</name>
  <files>auth/google_sheets.py, requirements.txt</files>
  <action>
In `auth/google_sheets.py`:

1. Remove the `from oauth2client.service_account import ServiceAccountCredentials` import entirely.
2. Remove `import json` if it's only used for credentials loading (it IS used in append_timestamp — keep it).
3. Remove the `SCOPES` list constant entirely.
4. Change the import from config to:
   ```python
   from config import (
       SHEET_COLUMNS,
       SHEET_NAME,
       WORKSPACES,
       TaskStatus,
       get_google_credentials,
   )
   ```
   (Remove `CREDENTIALS_FILE` import, add `get_google_credentials`.)

5. Rewrite `get_client()` to:
   ```python
   def get_client() -> gspread.Client:
       creds = get_google_credentials()
       if creds is None:
           raise RuntimeError("Google credentials not found in st.secrets")
       return gspread.service_account_from_dict(creds)
   ```
   Do NOT pass custom scopes — let service_account_from_dict use its defaults.

6. Update the module docstring to remove mention of "oauth2client".

In `requirements.txt`:
Remove the `oauth2client` line entirely. The file should contain only:
```
streamlit>=1.45
gspread>=6.0
pandas
```
  </action>
  <verify>
    1. `grep -c "oauth2client" auth/google_sheets.py` returns 0
    2. `grep -c "oauth2client" requirements.txt` returns 0
    3. `grep -c "ServiceAccountCredentials" auth/google_sheets.py` returns 0
    4. `grep -c "SCOPES" auth/google_sheets.py` returns 0
    5. `grep "service_account_from_dict" auth/google_sheets.py` succeeds
    6. `grep "get_google_credentials" auth/google_sheets.py` succeeds
    7. `grep "CREDENTIALS_FILE" auth/google_sheets.py` returns 0
    8. `python -c "from auth.google_sheets import get_client; print('OK')"` succeeds (may raise RuntimeError at runtime for missing creds, but import must work)
  </verify>
  <done>oauth2client is completely removed from all source files and requirements.txt. google_sheets.py uses gspread.service_account_from_dict() with credentials from get_google_credentials(). No SCOPES constant, no CREDENTIALS_FILE import.</done>
</task>

</tasks>

<verification>
1. `grep -r "oauth2client" --include="*.py" --include="*.txt" .` returns zero results
2. `grep -r "CREDENTIALS_FILE" --include="*.py" .` returns zero results
3. `python -c "from config import get_google_credentials, has_google_credentials"` succeeds
4. `python -c "from auth.google_sheets import get_client"` succeeds
</verification>

<success_criteria>
- config.py provides get_google_credentials() and has_google_credentials() with proper st.secrets error handling
- auth/google_sheets.py get_client() uses gspread.service_account_from_dict() exclusively
- oauth2client does not exist in requirements.txt or any .py file
- All existing google_sheets.py functions (read_projects, upsert_project, etc.) remain unchanged in signature and behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-secrets-management/06-01-SUMMARY.md`
</output>
