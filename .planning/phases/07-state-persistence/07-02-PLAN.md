---
phase: 07-state-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/workspace.py
  - components/project_card.py
autonomous: true

must_haves:
  truths:
    - "workspace.py calls rebuild_session_from_sheets() after loading data from Sheets"
    - "workspace.py calls force_flush() before reading data"
    - "project_card.py reads pause_reason from Sheets data preferentially"
    - "project_card.py uses debounced_write for status and timestamp updates"
  artifacts:
    - path: "components/workspace.py"
      provides: "Session rebuild on load + force flush before reads"
      contains: "rebuild_session_from_sheets"
    - path: "components/project_card.py"
      provides: "Sheets-first state with debounced writes"
      contains: "force_flush"
  key_links:
    - from: "components/workspace.py"
      to: "logic/state_manager.py"
      via: "import rebuild_session_from_sheets"
      pattern: "rebuild_session_from_sheets"
    - from: "components/project_card.py"
      to: "auth/google_sheets.py"
      via: "import force_flush, debounced_write"
      pattern: "force_flush"
---

<objective>
Wire workspace.py to rebuild session state from Sheets on every load, and update project_card.py to use debounced writes and prefer Sheets data for pause reasons.

Purpose: This completes the persistence loop — reads come from Sheets (source of truth), writes go through debounce buffer. Timer state, pause reasons, and project status all survive page refreshes.
Output: workspace.py with session rebuild, project_card.py with debounced writes.
</objective>

<execution_context>
@C:/Users/omarh/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/omarh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update workspace.py to rebuild session state and flush writes</name>
  <files>components/workspace.py</files>
  <action>
In `components/workspace.py`:

1. Add import: `from logic.state_manager import init_form_state, is_form_open, toggle_form, rebuild_session_from_sheets`
   (add `rebuild_session_from_sheets` to existing import)

2. Add import: `from auth.google_sheets import read_projects, upsert_project, force_flush`
   (add `force_flush` to existing import)

3. In `render_workspace()`, AFTER the `projects_df = read_projects(workspace_key)` line (and BEFORE `_render_status_bar`), add:
```python
    # Rebuild session state from Sheets (source of truth)
    if not projects_df.empty:
        rebuild_session_from_sheets(projects_df)
```

4. ALSO in `render_workspace()`, BEFORE the `read_projects()` call, add:
```python
    # Flush any pending writes before reading fresh data
    if not is_demo:
        force_flush()
```

The flow should be: force_flush() → read_projects() → rebuild_session_from_sheets() → render.
  </action>
  <verify>
    1. `grep "rebuild_session_from_sheets" components/workspace.py` succeeds
    2. `grep "force_flush" components/workspace.py` succeeds
    3. The force_flush call appears BEFORE read_projects in the code
    4. The rebuild_session call appears AFTER read_projects in the code
  </verify>
  <done>workspace.py flushes pending writes before reading, then rebuilds session state from Sheets data on every load.</done>
</task>

<task type="auto">
  <name>Task 2: Update project_card.py to prefer Sheets data and use force_flush</name>
  <files>components/project_card.py</files>
  <action>
In `components/project_card.py`:

1. Add `force_flush` to the import from `auth.google_sheets`:
```python
from auth.google_sheets import (
    append_timestamp,
    update_net_duration,
    update_pause_reason,
    force_flush,
)
```

2. In `_handle_action()`, add `force_flush()` call right after the `append_timestamp` write and before any reads:
```python
    if not is_demo:
        # Write to Google Sheets
        append_timestamp(workspace_key, project_id, event)
        force_flush()  # Ensure write is persisted before any read
```

This ensures that after every action (start/pause/resume/complete), the write is immediately flushed so that:
- The complete action's read-back sees the latest data
- Page refreshes immediately after an action show the correct state

Keep everything else in `_handle_action()` the same.
  </action>
  <verify>
    1. `grep "force_flush" components/project_card.py` succeeds
    2. `grep "force_flush()" components/project_card.py` count >= 1
    3. No changes to button rendering or card layout logic
  </verify>
  <done>project_card.py calls force_flush() after every write action, ensuring data is persisted immediately before any read-back or UI refresh.</done>
</task>

</tasks>

<verification>
1. `grep "rebuild_session_from_sheets" components/workspace.py` succeeds
2. `grep "force_flush" components/workspace.py` succeeds
3. `grep "force_flush" components/project_card.py` succeeds
4. App starts without import errors
</verification>

<success_criteria>
- workspace.py rebuilds session state from Sheets on every load
- workspace.py flushes pending writes before reading
- project_card.py force-flushes after every action write
- Timer state survives page refresh (PERS-01)
- Pause reasons survive page refresh (PERS-02)
- Fresh data on every load (PERS-03)
- Debounced writes available (PERS-04 infrastructure)
</success_criteria>

<output>
After completion, create `.planning/phases/07-state-persistence/07-02-SUMMARY.md`
</output>
